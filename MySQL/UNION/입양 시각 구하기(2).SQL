WITH RECURSIVE TIME AS (
    SELECT 
    0 AS HOUR
    
    UNION ALL
    
    SELECT HOUR + 1
    FROM TIME
    WHERE HOUR < 23
)

SELECT
HOUR,
(
    SELECT COUNT(*)
    FROM ANIMAL_OUTS AO
    WHERE HOUR(AO.DATETIME) = TIME.HOUR
) AS 'COUNT'
FROM TIME